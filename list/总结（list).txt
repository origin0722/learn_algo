# 列表是一种线性数据结构，它用一组连续的内存空间，来存储一组具有相同类型的数据
# 许多编程语言中的标准库提供的列表是基于动态数组实现的
# 动态数组，即数组长度可变的数组，在数组元素数量超过数组长度时，自动开辟新空间，将原数组元素搬迁至新空间
# 因此，列表的元素在内存中是连续存储的，且元素类型相同
# 列表的元素类型可以互不相同，例如列表中可以同时包含整数、浮点数、字符串、自定义对象等
# 列表的元素类型也可以是复合类型，例如列表中可以包含另一个列表
# 列表的元素类型也可以是复合类型，例如列表中可以包含另一个列表
# 列表的元素类型也可以是复合类型，例如列表中可以包含另一个列表
